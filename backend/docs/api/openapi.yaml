openapi: 3.1.0
info:
  title: Chura API
  description: Backend API for Chura — Project/Backlog/Sprint/Ticket Agile management.
  version: 0.1.0
servers:
  - url: http://localhost:8080
    description: Local development
paths:
  /health:
    get:
      summary: Health check
      operationId: getHealth
      responses:
        "200":
          description: Service is up
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
  schemas: {}
