// Package domain holds Chura's core entities and business rules
// (Project, Ticket, Sprint, User, Template, ...). It has no dependency
// on any framework, database, or transport concern.
package domain
