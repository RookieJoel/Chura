// Package out declares the driven (secondary) ports: the interfaces
// services depend on for persistence and other external systems.
// Adapters (postgres, mongodb, ...) implement these.
package out
